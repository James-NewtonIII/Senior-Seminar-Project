<%= form_with(model: taf_line_item, local: true) do |form| %>
  <% if taf_line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taf_line_item.errors.count, "error") %> prohibited this taf_line_item from being saved:</h2>

      <ul>
      <% taf_line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
